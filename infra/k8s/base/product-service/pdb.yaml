apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: product-service
  labels:
    app: product-service
    component: backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: product-service
