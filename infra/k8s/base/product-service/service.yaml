apiVersion: v1
kind: Service
metadata:
  name: product-service
  labels:
    app: product-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - name: http
      port: 80
      targetPort: http
