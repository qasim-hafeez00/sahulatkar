apiVersion: v1
kind: Service
metadata:
  name: web-customer
  labels:
    app: web-customer
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: web-customer
  ports:
    - name: http
      port: 80
      targetPort: http
