apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: web-customer
  labels:
    app: web-customer
    component: frontend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: web-customer
