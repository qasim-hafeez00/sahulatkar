apiVersion: v1
kind: Service
metadata:
  name: payment-orchestrator
  labels:
    app: payment-orchestrator
    component: backend
spec:
  type: ClusterIP
  selector:
    app: payment-orchestrator
  ports:
    - name: http
      port: 80
      targetPort: http
