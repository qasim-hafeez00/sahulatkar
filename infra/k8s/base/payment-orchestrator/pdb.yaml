apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: payment-orchestrator
  labels:
    app: payment-orchestrator
    component: backend
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: payment-orchestrator
