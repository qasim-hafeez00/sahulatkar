apiVersion: v1
kind: Service
metadata:
  name: gateway
  labels:
    app: gateway
    component: backend
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: http
      port: 80
      targetPort: http
