apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - hpa.yaml

images:
  - name: sk-credit-engine
    newName: sk-credit-engine
    newTag: latest
