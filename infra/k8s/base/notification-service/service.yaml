apiVersion: v1
kind: Service
metadata:
  name: notification-service
  labels:
    app: notification-service
    component: backend
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 80
      targetPort: http
