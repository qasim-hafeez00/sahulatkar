# Reference copy — place this in apps/gateway/ before running mutmut.
# The authoritative copy is already at apps/gateway/setup.cfg.

[mutmut]
paths_to_mutate=src/services/kyc.py
backup=False
runner=python -m pytest tests/test_services/test_kyc_service_unit.py -x --timeout=30 -q
tests_dir=tests/
