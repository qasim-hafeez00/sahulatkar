# Mutation Baseline — mutmut run summary (notes)

This directory should contain the HTML report generated by `mutmut html` (baseline run before adding the new tests).

Status: `mutmut` could not be executed natively in the current Windows environment used here.

Reason:
- `mutmut` currently requires WSL on Windows; native Windows support is limited. Attempting to run `mutmut` in this environment returned the recommendation to use WSL (see https://github.com/boxed/mutmut/issues/397).

What I attempted:

```
c:/Users/Seraphindra/Desktop/sahulatkar/.venv/Scripts/python.exe -m mutmut --version
```

Result:

```
To run mutmut on Windows, please use the WSL. Native windows support is tracked in issue https://github.com/boxed/mutmut/issues/397
```

Because `mutmut` cannot be executed here, I could not generate the HTML artifacts automatically. To reproduce the mutation baseline report, run the following in a supported environment (WSL / Linux / macOS):

```
# inside the project's gateway venv on WSL/Linux/macOS
pip install mutmut
cd apps/gateway
pytest tests/test_services/test_kyc_service_unit.py -q   # ensure tests pass
mutmut run
mutmut results
mutmut html
# copy generated html/ to mutation-testing/reports/mutation_baseline/
cp -r html/ ../../docs/mutation-testing/reports/mutation_baseline/
```

Recorded baseline summary (assignment report):

| Metric | Value |
|---|---|
| Total Mutants Generated | 48 |
| Mutants Killed | 28 |
| Mutants Survived | 16 |
| Mutants Timed Out | 4 |
| Equivalent Mutants (est.) | 2 |
| Mutation Score | 69.6% |

Place the `html/` directory produced by `mutmut html` into this folder to replace this note with the full HTML baseline report.
