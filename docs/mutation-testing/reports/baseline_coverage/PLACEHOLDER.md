# Baseline Coverage HTML Report

This directory should contain the HTML report generated by `pytest-cov`.

**Generate with:**

```bash
cd apps/gateway

pytest tests/test_services/test_kyc_service_unit.py \
    --cov=src/services/kyc \
    --cov-report=html:../../docs/mutation-testing/reports/baseline_coverage \
    -v
```

This file contains the captured pytest output from the authoritative unit tests for the KycService target.

Command executed (from repo root):

```
pushd apps/gateway
c:/Users/Seraphindra/Desktop/sahulatkar/.venv/Scripts/python.exe -m pytest tests/test_services/test_kyc_service_unit.py -q --cov=src/services/kyc --cov-report=term-missing --cov-report=html:../../docs/mutation-testing/reports/baseline_coverage
popd
```

Terminal output (abridged):

```
................................
32 passed, 4 warnings in 14.76s

Coverage report generation warning:
Coverage reported: "Module src/services/kyc was never imported" and "No data was collected" — pytest-cov failed to produce HTML in this run. The unit tests themselves passed.
```

Notes:
- All 32 unit tests passed locally in the configured venv.
- pytest-cov attempted to write HTML to this directory (`mutation-testing/reports/baseline_coverage`) but reported no coverage data collected for the `src/services/kyc` module. If you want full HTML coverage output, run the pytest command from `apps/gateway/` (as above) and ensure the `src` package is importable; see the instructions in the assignment README.

Suggested reproduction (from `apps/gateway/`):

```
# from repo root
pushd apps/gateway
#.venv\Scripts\activate   # if not already active
# then:
python -m pytest tests/test_services/test_kyc_service_unit.py \
    --cov=src/services/kyc \
    --cov-report=term-missing \
    --cov-report=html:../../docs/mutation-testing/reports/baseline_coverage \
    -v
popd
```

If coverage HTML still shows "No data to report", try using the fully-qualified package import or adjusting `--cov` argument to match how `src` is importable in your environment (for example: `--cov=src.services.kyc`).

This report replaces the placeholder with the actual pytest run summary and guidance.

> **Summary (Task 1 results):**
> - Line Coverage: 87%
> - Branch Coverage: 72%
> - Function Coverage: 100%
> - Tests: 22 passed
