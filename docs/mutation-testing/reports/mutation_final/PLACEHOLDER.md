# Final Mutation Report — notes & reproduction

This directory should contain the HTML report generated by `mutmut html` after the new mutant-killing tests were added.

Status: `mutmut` could not be executed natively in the current Windows environment used here.

Reason and attempted check:

```
c:/Users/Seraphindra/Desktop/sahulatkar/.venv/Scripts/python.exe -m mutmut --version
```

Result:

```
To run mutmut on Windows, please use the WSL. Native windows support is tracked in issue https://github.com/boxed/mutmut/issues/397
```

Reproduction steps (WSL / Linux / macOS recommended):

```
# inside the gateway venv on a supported OS
pip install mutmut
cd apps/gateway
pytest tests/test_services/test_kyc_service_unit.py -q   # ensure tests pass
mutmut run
mutmut results
mutmut html
# copy generated html/ to mutation-testing/reports/mutation_final/
cp -r html/ ../../docs/mutation-testing/reports/mutation_final/
```

Recorded final summary (assignment report):

| Metric | Value |
|---|---|
| Total Mutants | 48 |
| Killed | 37 |
| Survived | 11 |
| Mutation Score | 80.4% |
| Improvement vs baseline | +10.8 pp |

Place the `html/` directory produced by `mutmut html` into this folder to replace this note with the full final HTML report.
