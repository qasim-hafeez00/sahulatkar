[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sk-ledger-service"
version = "0.1.0"
description = "ledger-service for SahulatKar"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110.0",
    "uvicorn[standard]>=0.29.0",
    "sk-shared"
]
